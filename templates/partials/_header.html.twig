<header class="bg-white fixed md:static bottom-0 right-0 left-0 shadow-md rotate-180 md:rotate-0 z-10">
	<div class="md:container mx-auto rotate-180 md:rotate-0">
		<div class="flex justify-between items-center">
			<a class="hidden md:inline p-4 hover:bg-neutral-100 active:bg-neutral-200 transition m-2 rounded-sm" href="{{ path('home.home') }}"><img src="{{asset('logo.svg')}}" alt="Logo" class="w-24"></a>
			<nav class="font-medium text-lg flex justify-around w-full md:justify-end">
				<a class="w-full md:w-auto flex justify-center hover:bg-neutral-100 active:bg-neutral-200 py-2 px-6 rounded-sm" href="{{ path('home.home') }}">
					<span class="md:hidden">
						<i class="fa-regular fa-xl p-8 fa-house"></i>
					</span>
					<span class="hidden md:inline">Accueil</span>
				</a>
				{% if is_granted('ROLE_USER') %}
					<a class="w-full md:w-auto flex justify-center hover:bg-neutral-100 active:bg-neutral-200 py-2 px-6 rounded-sm" href="{{ path('security.logout') }}">
						<span class="md:hidden">
							<i class="fa-regular fa-xl p-8 fa-right-from-bracket"></i>
						</span>
						<span class="hidden md:inline">Se déconnecter</span>
					</a>
				{% else %}
					<a class="w-full md:w-auto flex justify-center hover:bg-neutral-100 active:bg-neutral-200 py-2 px-6 rounded-sm" href="{{ path('security.login') }}">
						<span class="md:hidden">
							<i class="fa-regular fa-xl p-8 fa-right-to-bracket"></i>
						</span>
						<span class="hidden md:inline">Se connecter</span>
					</a>
					<a class="w-full md:w-auto flex justify-center hover:bg-neutral-100 active:bg-neutral-200 py-2 px-6 rounded-sm" href="{{ path('security.register') }}">
						<span class="md:hidden">
							<i class="fa-regular fa-xl p-8 fa-user-plus"></i>
						</span>
						<span class="hidden md:inline">Créer un compte</span>
					</a>
				{% endif %}
			</nav>
		</div>
	</div>
</header>
